/**! compression tag for ftp-deployment */

/**
 * Ready function
 */
(function ($) {
    'use strict';

    // tooltip
    $('body').customTooltip({
        selector: '.has-tooltip',
        type: 'danger overall-card'
    });
}(jQuery));
