{% apply spaceless %}
    {% set image = vich_uploader_asset(user)|replace({'192': '032'}) %}
    {% if image -%}
        {{ asset_image(image, {'alt': 'user.image.title'|trans, 'title': 'user.image.title'|trans, 'class': 'avatar'}) }}
    {% endif %}
{% endapply %}
