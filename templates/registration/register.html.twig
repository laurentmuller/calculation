{% extends 'cards/card_edit_center.html.twig' %}
{% from 'macros/_icons.html.twig' import link %}
{# parameters #}
{%- set title = 'registration.register.title' -%}
{%- set title_icon = 'user-plus' -%}
{%- set submit_text = 'registration.register.submit' -%}
{%- set cancel_path = false -%}
{%- set form_attr = form_attr|default({})|merge({'data-equal_to': 'password.mismatch'|trans([], 'validators')}) -%}
{# actions #}
{% block actions_form %}
    {{ parent() }}
    {{ link(
        href: path('app_login'),
        text: 'security.login.submit',
        class: 'btn btn-form btn-secondary',
        icon: 'sign-in-alt',
        title: 'security.login.description',
        inline: true) }}
    {{ link(
        href: path('app_forgot_password_request'),
        text: 'resetting.link',
        class: 'btn btn-form btn-outline-secondary',
        icon: 'circle-question',
        title: 'resetting.request.description',
        inline: true) }}
{% endblock actions_form %}
{# javascripts #}
{% block javascripts %}
    {% if is_js_debug %}
        {{ parent() }}
        {{ asset_js('js/plugins/plugin-password-strength.js') }}
        {{ asset_js('js/extensions/password-extensions.js') }}
        {{ asset_js('js/application/user_register.js') }}
    {% else %}
        {{ asset_js('js/compiled/user_register.js') }}
    {% endif %}
{%- endblock javascripts %}
