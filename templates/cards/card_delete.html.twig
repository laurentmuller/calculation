{% extends 'cards/card_confirm.html.twig' %}
{# parameters #}
{%- set title = title|default('common.delete_title') -%}
{%- set title_icon = title_icon|default(ICON_DELETE) -%}
{%- set submit_text = submit_text|default('common.button_delete') -%}
{%- set border = 'danger' -%}
