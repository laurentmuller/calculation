{% extends 'chart/chart_base.html.twig' %}
{% from 'macros/_properties.html.twig' import stateCss %}
{% from 'macros/_icons.html.twig' import button_pdf %}
{% trans_default_domain 'chart' %}
{# parameters #}
{%- set title = 'title_by_state' -%}
{%- set title_icon = ICON_CALCULATION_STATE -%}
{# class #}
{% block card_container_class '' -%}
{# data #}
{% block data %}
<thead>
    <tr>
        <th class="text-nowrap">{{ 'fields.state'|trans }}</th>
        <th class="text-currency">{{ 'fields.count'|trans }}</th>
        <th class="text-percent text-center">%</th>
        <th class="text-currency">{{ 'fields.net'|trans }}</th>
        <th class="text-currency">{{ 'fields.margin_amount'|trans }}</th>
        <th class="text-percent">{{ 'fields.margin_percent'|trans }}</th>
        <th class="text-currency">{{ 'fields.total'|trans }}</th>
        <th class="text-percent text-center">%</th>
    </tr>
</thead>
<tbody data-link="row" class="rowlink">
    {% for item in data -%}
    <tr title="{{ 'row_by_state'|trans({'%name%': item.code}) }}">
        <td class="text-nowrap">
            <div class="d-flex align-items-center">
                <span class="border me-1" style="width: 1.5rem; height: 1rem; background: {{ item.color }}"></span>
                <a href="{{ path(list_path, {'stateId': item.id}) }}">{{- item.code }}</a>
            </div>
        </td>
        <td class="text-currency">{{ item.count|integer }}</td>
        <td class="text-percent">{{ item.percentCalculation|percent(false, 2, 4) }}</td>
        <td class="text-currency">{{ item.items|integer }}</td>
        <td class="text-currency">{{ item.marginAmount|integer }}</td>
        <td class="text-percent{{ _self.margin_below_class(item.margin, min_margin) }}"{{ _self.margin_below_tooltip(item.margin, min_margin) }}>{{ item.margin|percent(false) }}</td>
        <td class="text-currency">{{ item.total|integer }}</td>
        <td class="text-percent">{{ item.percentAmount|percent(false, 2, 4) }}</td>
    </tr>
    {% endfor -%}
    <tr title="{{ 'row_show_all'|trans }}">
        <td class="fw-bold text-nowrap">
            <a href="{{ path(list_path) }}">{{ 'fields.total'|trans }}</a>
        </td>
        <td class="fw-bold text-currency">{{ count|integer }}</td>
        <td class="fw-bold text-percent">{{ 1.0|percent(false, 2, 4) }}</td>
        <td class="fw-bold text-currency">{{ items|integer }}</td>
        <td class="fw-bold text-currency">{{ marginAmount|integer }}</td>
        <td class="fw-bold text-percent{{ _self.margin_below_class(margin, min_margin) }}"{{ _self.margin_below_tooltip(margin, min_margin) }}>{{- margin|percent(false) -}}</td>
        <td class="fw-bold text-currency">{{ total|integer }}</td>
        <td class="fw-bold text-percent">{{ 1.0|percent(false, 2, 4) }}</td>
    </tr>
</tbody>
{% endblock %}
{# footer #}
{% block card_footer -%}
    {{ parent() }}
    {% if is_granted(ATTRIBUTE_EXPORT, ENTITY_CALCULATION) %}
        {{ button_pdf('chart_state_pdf', {}, 'common.button_export_pdf') }}
    {% endif %}
{%- endblock %}
