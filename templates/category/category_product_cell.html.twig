{%- if count > 0 -%}
{%- set href = path('product_table', {'query': code}) -%}
<a href="{{ href }}" title="{{ 'category.list.product_title'|trans }}">{{ count|integer }}</a>
{%- else -%}
{{ count|integer }}
{%- endif -%}
