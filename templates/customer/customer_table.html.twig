{% extends 'cards/card_table.html.twig' %}

{# parameters #}
{%- set title       = 'customer.list.title' -%}
{%- set title_icon  = 'address-card far' -%}
{%- set pdf_title   = 'customer.list.pdf_title' -%}
{%- set object_type = IEntityVoter.ENTITY_CUSTOMER -%}

{# actions #}
{%- set params      = {'id' : 0} -%}
{%- set add_path    = path('customer_add') -%}
{%- set show_path   = path('customer_show', params) -%}
{%- set edit_path   = path('customer_edit', params) -%}
{%- set delete_path = path('customer_delete', params) -%}
{%- set pdf_path    = path('customer_pdf') -%}

{# titles #}
{%- set add_title    = 'customer.add.title' %}
{%- set show_title   = 'customer.show.title' %}
{%- set edit_title   = 'customer.edit.title' %}
{%- set delete_title = 'customer.delete.title' %}

{# highlight search #}
{%- set attributes = attributes|default({})|merge ({
    'data-search-highlight': true
}) -%}
