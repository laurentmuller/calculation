/**! compression tag for ftp-deployment */

/**
 * Ready function
 */
(function ($) {
    'use strict';

    // tooltip
    $('body').tooltip({
        selector: '.has-tooltip',
        customClass: 'tooltip-danger overall-card'
    });
}(jQuery));
