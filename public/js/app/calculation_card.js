/**! compression tag for ftp-deployment */

/**
 * Ready function
 */
$(function () {
    'use strict';

    // tooltip
    $('body').customTooltip({
        selector: '.has-tooltip',
        className: 'tooltip-danger overall-card'
    });
});