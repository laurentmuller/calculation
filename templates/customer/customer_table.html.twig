{% extends 'cards/card_table.html.twig' %}

{# parameters #}
{%- set title = 'customer.list.title' -%}
{%- set title_icon = ICON_CUSTOMER -%}
{%- set object_type = ENTITY_CUSTOMER -%}

{% block header_actions -%}
{% if allow_add %}<a class="add-link p-0" href="{{ path('customer_add', params) }}">{{ 'customer.add.title'|trans }}</a>{% endif %}
{%- endblock %}

{% block global_actions -%}
{{- _self.item_external(path('customer_pdf'), 'customer.list.pdf_title', 'file-pdf far') }}
{{- _self.item_external(path('customer_excel'), 'customer.list.excel_title', 'file-excel far') }}
{%- endblock %}

{% block show_actions -%}
{{- _self.item_path(path('customer_show', params), 'customer.show.title', 'tv', show_class) }}
{%- endblock %}

{% block edit_actions -%}
{{- _self.item_path(path('customer_edit', params), 'customer.edit.title', 'pencil', edit_class) }}
{%- endblock %}

{% block delete_actions -%}
{{- _self.item_path(path('customer_delete', params), 'customer.delete.title', 'times', 'btn-delete') }}
{%- endblock %}

{% block add_actions -%}
{{- _self.item_separator() }}
{{- _self.item_path(path('customer_add', params), 'customer.add.title', 'file far', 'btn-add') }}
{%- endblock %}

{% block custom_view_header -%}
{%- if row_edit -%}
    <a href="{{ path('customer_edit', params) }}" class="item-link text-body" title="{{ 'customer.edit.title'|trans }}">%nameAndCompany%</a>
{%- elseif row_show -%}
    <a href="{{ path('customer_show', params) }}" class="item-link text-body" title="{{ 'customer.show.title'|trans }}">%nameAndCompany%</a>
{%- else -%}
    <span>%nameAndCompany%</span>
{%- endif -%}
{%- endblock %}

{% block custom_view_body -%}
{{ _self.property_value('%address%') }}
{{ _self.property_value('%zipCity%') }}
{%- endblock %}
