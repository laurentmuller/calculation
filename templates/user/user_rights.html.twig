{% extends 'cards/card_rights.html.twig' %}
{# parameters #}
{%- set title = 'user.rights.title' -%}
{%- set page_list = 'user_index' -%}
{# body #}
{% block card_body %}
    <div class="row">
        <div class="col-md-5">
            {{ form_row(form.username) }}
        </div>
        <div class="col-md-4">
            {{ form_row(form.role) }}
        </div>
        <div class="col-md-3">
            {{ form_row(form.enabled) }}
        </div>
    </div>
    {{ _self.table(form.rights, permissions, overwrite) }}
    {{ form_row(form.overwrite, {attr: {'data-default': default_overwrite|json_encode}}) }}
    {{ parent() }}
{% endblock card_body %}
