{
    "name": "custom_bootstrap",
    "version": "1.0.0",
    "description": "Extends Bootstrap with no rounded and themed popovers and tooltips and form-check.",
    "license": "MIT",
    "author": "bibi@bibi.nu",
    "type": "commonjs",
    "dependencies": {
        "bootstrap": "^5.3"
    },
    "scripts": {
        "watch": "sass --watch --no-source-map scss/bootstrap.scss:public/vendor/bootstrap/css/bootstrap.css",
        "build": "sass --no-source-map scss/bootstrap.scss:public/vendor/bootstrap/css/bootstrap.css",
        "stylelint": "npx stylelint public/css/*.css"
    },
    "devDependencies": {
        "stylelint": "^16.0.0",
        "stylelint-config-standard": "^37.0.0"
    }
}
